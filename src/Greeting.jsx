// 8. Refactor with Keys
// Task: Use a list of objects with unique IDs:
// const animals = [{ name: "Lion", id: "a1" }, { name: "Tiger", id: "a2" }]
// ✅ Render them in a list using their id as key.
// 🔁 Then change the list to use index as a key and try reordering to see what happens.

// 9. Clickable Cards
// Task: Create a Card component that accepts a title and an onClick handler.
// ✅ When clicked, it should alert the title.
// ✅ Pass the function from the parent (App), using a parameter.

